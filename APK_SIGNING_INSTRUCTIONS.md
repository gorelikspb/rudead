# 🔐 Как получить подписанный APK для установки

## ❌ Проблема
APK неподписанный, поэтому Android не может его установить. Ошибка: "пакет невалиден" или "package is invalid".

## ✅ Решение 1: Пересобрать подписанный APK через PWA Builder (РЕКОМЕНДУЕТСЯ)

### Шаги:

1. **Открой PWA Builder:**
   - Зайди на https://www.pwabuilder.com/
   - Введи URL: `https://areyoudeadapp.pages.dev`
   - Нажми "Start"

2. **Нажми "Package For Stores" → "Android"**

3. **ВАЖНО: Выбери "Trusted Web Activity (TWA)"**

4. **В диалоге "Android Options" (или настройках):**
   - **Package name:** `com.areyoudeadapp.app`
   - **App name:** `Are You Dead?`
   - **Version:** `1.0.0`
   - **Version code:** `1`
   - **✅ ВАЖНО: Выбери "Generate signed package" или "Sign APK"**
     - Если есть опция "Signing key" → выбери "Create new key" или "Use existing key"
     - Если создаешь новый ключ - **ОБЯЗАТЕЛЬНО сохрани его!** Он понадобится для обновлений

5. **Нажми "Generate Package"**

6. **Скачай ZIP файл**

7. **Распакуй ZIP и найди APK:**
   - В распакованном архиве найди файл `.apk`
   - Обычно это `app-release-signed.apk` или `app-release.apk` в папке `android/app/build/outputs/apk/release/`

8. **Проверь установку:**
   - Скопируй APK на телефон
   - Попробуй установить
   - Если установка работает - APK правильный!

9. **Замени файл на сайте:**
   ```bash
   # Переименуй APK
   # Скопируй в public/
   cp путь/к/app-release-signed.apk public/are-you-dead-app.apk
   
   # Закоммить
   git add public/are-you-dead-app.apk
   git commit -m "Replace with signed APK"
   git push origin main
   ```

---

## ✅ Решение 2: Подписать APK вручную (если уже есть APK)

Если у тебя уже есть APK файл, но он неподписанный:

### Вариант A: Через PWA Builder (проще)

1. Зайди на PWA Builder
2. Вместо генерации нового пакета, используй опцию "Sign existing APK"
3. Загрузи свой APK
4. Создай новый ключ подписи (или используй существующий)
5. Скачай подписанный APK

### Вариант B: Через командную строку (требует Android SDK)

```bash
# 1. Создай ключ подписи (если еще нет)
keytool -genkey -v -keystore my-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias my-key-alias

# 2. Подпиши APK
jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 -keystore my-release-key.jks are-you-dead-app.apk my-key-alias

# 3. Проверь подпись
jarsigner -verify -verbose -certs are-you-dead-app.apk
```

**⚠️ ВАЖНО:** Сохрани файл `my-release-key.jks` и пароль! Они понадобятся для всех будущих обновлений APK.

---

## 🔍 Как проверить что APK подписан:

### На Windows (если установлен Android SDK):
```bash
apksigner verify --print-certs public/are-you-dead-app.apk
```

### На Android устройстве:
- Попробуй установить APK
- Если установка проходит без ошибок "invalid package" - APK подписан правильно

---

## 📝 Чеклист:

- [ ] Пересобран APK через PWA Builder с опцией "Sign APK"
- [ ] Сохранен ключ подписи (если создавался новый)
- [ ] Проверена установка APK на телефон
- [ ] Заменен файл `public/are-you-dead-app.apk`
- [ ] Закоммичен и запушен новый APK
- [ ] Проверено что APK скачивается и устанавливается с сайта

---

## 🚨 Важные замечания:

1. **Ключ подписи:**
   - Если создаешь новый ключ - **ОБЯЗАТЕЛЬНО сохрани его!**
   - Без ключа ты не сможешь обновлять APK в будущем
   - Сохрани: файл `.jks` или `.keystore`, пароль, alias

2. **Для Google Play Store:**
   - Если планируешь выкладывать в Google Play - нужен подписанный APK
   - Google Play требует подпись для всех приложений

3. **Для прямого скачивания:**
   - Подписанный APK можно устанавливать напрямую на Android
   - Не нужен Google Play Store

---

## 💡 Рекомендация:

**Используй Решение 1** (пересобрать через PWA Builder с подписью) - это самый простой и надежный способ.

