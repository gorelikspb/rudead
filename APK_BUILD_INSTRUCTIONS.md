# 📱 Как создать APK из PWA (самый простой способ)

## 🎯 Вариант 1: PWA Builder (Microsoft) - РЕКОМЕНДУЕТСЯ

### Шаги:

1. **Открой PWA Builder:**
   - Зайди на https://www.pwabuilder.com/
   - Введи URL: `https://areyoudeadapp.pages.dev`
   - Нажми "Start"

2. **Собери APK:**
   - Выбери "Android" → "Generate Package"
   - Выбери "Trusted Web Activity (TWA)" - это важно!
   - Заполни информацию:
     - Package name: `com.areyoudeadapp.app` (или свой)
     - App name: `Are You Dead?`
     - Version: `1.0.0`
   - Нажми "Generate Package"
   - Скачай ZIP файл

3. **Извлеки APK:**
   - Распакуй ZIP
   - Найди файл `app-release.apk` в папке `android/app/build/outputs/apk/release/`
   - Это твой готовый APK!

### ✅ Преимущества TWA:
- **Автоматическое обновление** - APK загружает контент с твоего сайта, поэтому обновления происходят автоматически без переустановки APK
- **Простота** - не нужно пересобирать APK при каждом обновлении
- **Малый размер** - APK это просто обертка (~2-5 MB)

---

## 🎯 Вариант 2: Bubblewrap (Google) - через командную строку

Если предпочитаешь командную строку:

```bash
# Установка
npm install -g @bubblewrap/cli

# Инициализация проекта
bubblewrap init --manifest https://areyoudeadapp.pages.dev/manifest.json

# Сборка APK
bubblewrap build
```

APK будет в папке `android/app/build/outputs/apk/release/app-release.apk`

---

## 📤 Как добавить APK на сайт:

1. **Загрузи APK в папку `public/`:**
   ```
   public/
     └── are-you-dead-app.apk
   ```

2. **Обнови страницу `/download`:**
   - Добавь кнопку скачивания APK
   - Добавь инструкции по установке

3. **Обнови `sitemap.xml`:**
   - Добавь ссылку на APK файл

---

## 🔄 Как работает автообновление:

**TWA (Trusted Web Activity)** - это обертка вокруг твоего PWA:
- APK содержит только минимальный код для запуска браузера
- Весь контент загружается с твоего сайта `https://areyoudeadapp.pages.dev`
- Когда ты обновляешь сайт - пользователи автоматически получают новую версию
- **Не нужно пересобирать APK** при каждом обновлении!

**Единственный случай, когда нужно пересобрать APK:**
- Если меняешь `package name` или `signing key`
- Если добавляешь новые разрешения (permissions)
- Если меняешь иконку или название приложения

---

## 📝 Что нужно сделать после создания APK:

1. ✅ Загрузить APK в `public/are-you-dead-app.apk`
2. ✅ Обновить страницу `/download` с кнопкой скачивания
3. ✅ Добавить инструкции по установке APK
4. ✅ Обновить `version.json` при каждом обновлении сайта (для отслеживания)

---

## 🚀 Готово!

Теперь у тебя будет:
- PWA версия (для большинства пользователей)
- APK версия (для тех, кто ищет "apk download")
- Автоматическое обновление через TWA

---

## 📋 Дальнейшие действия (ШАГ ЗА ШАГОМ):

### Шаг 1: В PWA Builder нажми "Package For Stores"
- Кнопка должна быть в правом верхнем углу или в боковой панели
- Выбери "Android"

### Шаг 2: Выбери "Trusted Web Activity (TWA)"
- Это важно! TWA обеспечивает автоматическое обновление
- Не выбирай "Capacitor" или другие варианты

### Шаг 3: Заполни информацию о приложении:
- **Package name:** `com.areyoudeadapp.app` (или свой, но должен быть уникальным)
- **App name:** `Are You Dead?`
- **Version:** `1.0.0`
- **Version code:** `1`
- Остальные поля заполни по желанию

### Шаг 4: Нажми "Generate Package"
- PWA Builder создаст ZIP файл с APK
- Скачай ZIP файл

### Шаг 5: Извлеки APK из ZIP:
- Распакуй ZIP файл
- Найди файл `app-release.apk` в папке:
  ```
  android/app/build/outputs/apk/release/app-release.apk
  ```
- Или просто найди файл `.apk` в распакованной папке

### Шаг 6: Переименуй и загрузи APK:
- Переименуй `app-release.apk` в `are-you-dead-app.apk`
- Загрузи файл в папку `public/`:
  ```
  public/
    └── are-you-dead-app.apk
  ```

### Шаг 7: Закоммить и запушить:
```bash
git add public/are-you-dead-app.apk
git commit -m "Add APK file for download"
git push origin main
```

### Шаг 8: Проверь что работает:
- Подожди 1-2 минуты пока Cloudflare обновит сайт
- Открой: `https://areyoudeadapp.pages.dev/download`
- Кнопка "Download APK" должна работать
- Проверь что файл скачивается: `https://areyoudeadapp.pages.dev/are-you-dead-app.apk`

---

## ✅ Готово!

Теперь пользователи смогут:
- Скачать APK напрямую с сайта
- APK будет автоматически обновляться (благодаря TWA)
- Или установить PWA версию через браузер

**Важно:** При каждом обновлении сайта APK автоматически получит новые функции - не нужно пересобирать APK!

