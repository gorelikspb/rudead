# 🔧 Исправление ошибки установки APK

## ❌ Проблема
Ошибка при установке: "Die App wurde nicht installiert, da das Paket offenbar ungültig ist" (The app was not installed because the package is apparently invalid).

## 🔍 Возможные причины:
1. APK поврежден при загрузке/скачивании
2. APK неправильно подписан
3. Неправильный MIME type при отдаче файла
4. Проблема со сборкой APK в PWA Builder

## ✅ Решение:

### ⚠️ ГЛАВНАЯ ПРОБЛЕМА: APK неподписанный!

PWA Builder сгенерировал **неподписанный APK**, поэтому Android не может его установить. Нужно пересобрать APK **с подписью**.

### Шаг 1: Пересобрать ПОДПИСАННЫЙ APK через PWA Builder

1. **Открой PWA Builder:**
   - Зайди на https://www.pwabuilder.com/
   - Введи URL: `https://areyoudeadapp.pages.dev`
   - Нажми "Start"

2. **Проверь что все тесты пройдены:**
   - Manifest валидный ✅
   - Service Worker работает ✅
   - HTTPS включен ✅
   - Иконки доступны ✅

3. **Собери ПОДПИСАННЫЙ APK:**
   - Нажми "Package For Stores" → "Android"
   - Выбери **"Trusted Web Activity (TWA)"** (НЕ Capacitor!)
   - **В диалоге "Android Options":**
     - **Package name:** `com.areyoudeadapp.app`
     - **App name:** `Are You Dead?`
     - **Version:** `1.0.0`
     - **Version code:** `1`
     - **✅ КРИТИЧЕСКИ ВАЖНО: Выбери "Generate signed package" или "Sign APK"**
     - Если есть опция "Signing key" → выбери "Create new key"
     - **Сохрани ключ подписи!** Он понадобится для обновлений
   - Нажми "Generate Package"

4. **Скачай и проверь APK:**
   - Скачай ZIP файл
   - Распакуй ZIP
   - Найди `app-release.apk` в папке `android/app/build/outputs/apk/release/`
   - **Важно:** Перед загрузкой попробуй установить APK на телефон напрямую (через USB или файловый менеджер)
   - Если установка работает - значит APK правильный

### Шаг 2: Замени APK файл

1. **Переименуй новый APK:**
   - Переименуй `app-release.apk` → `are-you-dead-app.apk`

2. **Замени файл:**
   ```bash
   # Удали старый APK
   rm public/are-you-dead-app.apk
   
   # Скопируй новый APK в public/
   cp путь/к/app-release.apk public/are-you-dead-app.apk
   ```

3. **Закоммить:**
   ```bash
   git add public/are-you-dead-app.apk
   git commit -m "Fix: Replace corrupted APK with newly generated one"
   git push origin main
   ```

### Шаг 3: Проверь что работает

1. **Проверь MIME type:**
   - Открой в браузере: `https://areyoudeadapp.pages.dev/are-you-dead-app.apk`
   - В DevTools (Network) проверь что `Content-Type: application/vnd.android.package-archive`
   - Файл `public/_headers` уже настроен для правильного MIME type

2. **Проверь установку:**
   - Подожди 1-2 минуты после пуша (Cloudflare обновит сайт)
   - Скачай APK с сайта на телефон
   - Попробуй установить

## 🛠️ Альтернативное решение: Используй Bubblewrap (CLI)

Если PWA Builder продолжает давать ошибки, попробуй Bubblewrap:

```bash
# Установка
npm install -g @bubblewrap/cli

# Инициализация (если еще не делал)
bubblewrap init --manifest https://areyoudeadapp.pages.dev/manifest.json

# Сборка APK (с правильной подписью)
bubblewrap build --skipVersionCheck

# APK будет в:
# android/app/build/outputs/apk/release/app-release.apk
```

## 📝 Чеклист:

- [ ] Пересобран APK через PWA Builder
- [ ] Проверена установка APK локально на телефон
- [ ] Заменен файл `public/are-you-dead-app.apk`
- [ ] Создан/обновлен файл `public/_headers` для правильного MIME type
- [ ] Закоммичен и запушен новый APK
- [ ] Проверено что APK скачивается с сайта
- [ ] Проверено что установка работает на Android

## 🔍 Дополнительная диагностика:

Если проблема сохраняется:

1. **Проверь размер файла:**
   - APK должен быть примерно 800KB - 2MB для TWA
   - Если меньше 100KB - файл поврежден

2. **Проверь что APK подписан:**
   ```bash
   # На Windows с Android SDK:
   apksigner verify --print-certs public/are-you-dead-app.apk
   ```

3. **Проверь логи браузера:**
   - В Chrome DevTools → Network → проверь Response Headers
   - Должен быть: `Content-Type: application/vnd.android.package-archive`

4. **Проверь настройки безопасности Android:**
   - Settings → Security → Unknown Sources (разрешена установка из неизвестных источников)

