# 📧 Решения для отправки тестовых email

## Текущая ситуация

**Resend в тестовом режиме:**
- ✅ Позволяет отправлять только на `gorelikgo@gmail.com`
- ❌ Не позволяет отправлять на любые email без верификации домена
- ✅ Бесплатно: 3,000 писем/месяц

## Решение 1: Worker с fallback (текущее)

**Как работает:**
1. Worker пытается отправить тестовое письмо на email контакта
2. Если Resend не позволяет (тестовый режим) → отправляет на `gorelikgo@gmail.com` с пометкой
3. В письме указано, для какого email оно предназначалось

**Плюсы:**
- ✅ Работает сразу, без дополнительной настройки
- ✅ Бесплатно
- ✅ Пользователь видит пример письма

**Минусы:**
- ❌ Письмо приходит на `gorelikgo@gmail.com`, а не на email контакта
- ❌ Нужно вручную пересылать или объяснять пользователю

---

## Решение 2: EmailJS для тестовых писем (рекомендуется)

**EmailJS:**
- ✅ Бесплатно: 200 писем/месяц
- ✅ Можно отправлять на любые email сразу
- ✅ Не требует верификации домена
- ✅ Простая интеграция

### Настройка EmailJS для тестовых писем:

1. **Регистрация:**
   - Зайди на https://www.emailjs.com
   - Создай бесплатный аккаунт

2. **Подключи Gmail:**
   - Dashboard → Email Services → Add New Service → Gmail
   - Скопируй Service ID

3. **Получи Public Key:**
   - Dashboard → Account → General
   - Скопируй Public Key

4. **Создай шаблон:**
   - Dashboard → Email Templates → Create New Template
   - Название: "Test Email Preview"
   - To Email: `{{to_email}}`
   - Subject: `🧪 TEST: R U Dead? - Preview`
   - Content: используй переменные `{{to_name}}`, `{{days_overdue}}`, `{{last_checkin}}`
   - Скопируй Template ID

5. **Добавь в Worker Secrets:**
   - `EMAILJS_PUBLIC_KEY` = твой Public Key
   - `EMAILJS_SERVICE_ID` = твой Service ID
   - `EMAILJS_TEMPLATE_ID` = твой Template ID

6. **Обнови Worker код:**
   - Добавь обработку EmailJS для типа 'test'
   - См. пример кода ниже

### Код для Worker (добавить в worker-code.js):

```javascript
// Test email via EmailJS (can send to any email)
if (data.type === 'test') {
  // Use EmailJS for test emails (works with any email)
  const emailjsUrl = `https://api.emailjs.com/api/v1.0/email/send`;
  
  const emailjsResponse = await fetch(emailjsUrl, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      service_id: env.EMAILJS_SERVICE_ID,
      template_id: env.EMAILJS_TEMPLATE_ID,
      user_id: env.EMAILJS_PUBLIC_KEY,
      template_params: {
        to_name: data.contact_name,
        to_email: data.contact_email,
        days_overdue: data.days_overdue,
        last_checkin: data.last_checkin,
        contact_phone: data.contact_phone
      }
    })
  });
  
  const emailjsResult = await emailjsResponse.json();
  
  if (emailjsResponse.ok) {
    return new Response(JSON.stringify({ 
      success: true,
      sentTo: data.contact_email,
      method: 'EmailJS'
    }), {
      headers: {
        'Content-Type': 'application/json',
        'Access-Control-Allow-Origin': '*'
      }
    });
  } else {
    // Fallback to Resend if EmailJS fails
    // ... (код fallback)
  }
}
```

**Плюсы:**
- ✅ Отправляет на реальный email контакта
- ✅ Бесплатно (200 писем/месяц)
- ✅ Не требует верификации домена

**Минусы:**
- ❌ Нужна дополнительная настройка
- ❌ Лимит 200 писем/месяц (но для тестов достаточно)

---

## Решение 3: Верифицировать домен в Resend (для продакшена)

**Для продакшена:**
1. Открой: https://resend.com/domains
2. Нажми **Add Domain**
3. Добавь свой домен (например, `rudead.com`)
4. Добавь DNS записи в Cloudflare
5. Подожди верификации (5-10 минут)
6. В Worker измени `from: 'R U Dead? <noreply@rudead.com>'`
7. Теперь можно отправлять на любые email

**Плюсы:**
- ✅ Можно отправлять на любые email
- ✅ Профессиональный отправитель
- ✅ 3,000 писем/месяц бесплатно

**Минусы:**
- ❌ Нужен домен
- ❌ Нужна настройка DNS

---

## Рекомендация

**Для тестовых писем:**
- Используй **EmailJS** (Решение 2) - отправляет на реальный email контакта бесплатно

**Для продакшена:**
- Верифицируй домен в **Resend** (Решение 3) - профессиональное решение

**Текущее решение (Worker с fallback):**
- Работает, но письма приходят на `gorelikgo@gmail.com`
- Можно использовать пока не настроен EmailJS

---

## Быстрая настройка EmailJS (5 минут)

1. Регистрация на emailjs.com
2. Подключи Gmail → скопируй Service ID
3. Скопируй Public Key
4. Создай шаблон → скопируй Template ID
5. Добавь Secrets в Worker
6. Обнови Worker код (см. пример выше)

**Готово!** Теперь тестовые письма будут отправляться на реальный email контакта.

